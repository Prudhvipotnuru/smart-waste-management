spring:
  application:
    name: swacch
  datasource:
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    url: ${SPRING_DATASOURCE_URL}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: never # or: embedded, always
  mvc:
    view:
      suffix: ".html"
  mail:
    host: smtp.gmail.com
    port: 587
    username: prudhvipotnuru11@gmail.com
    password: tpfkveglrinwqmkk
    properties:
      mail:
        smtp:
          auth: true
          starttls: 
            enable: true
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE
      springframework: INFO
    com:
      prudhvi:
        swacch: DEBUG